import { ComponentType } from "react";
type Props = {
    live: Record<string, unknown>;
};
export default function withLive<T>(WrappedComponent: ComponentType<T & Props>): {
    (props: T): JSX.Element;
    displayName: string;
};
export {};
