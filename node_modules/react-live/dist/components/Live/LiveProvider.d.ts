import { PropsWithChildren } from "react";
import type { Language, PrismTheme } from "prism-react-renderer";
type Props = {
    code?: string;
    disabled?: boolean;
    enableTypeScript?: boolean;
    language?: Language;
    noInline?: boolean;
    scope?: Record<string, unknown>;
    theme?: PrismTheme;
    transformCode?(code: string): void;
};
declare function LiveProvider({ children, code, language, theme, enableTypeScript, disabled, scope, transformCode, noInline, }: PropsWithChildren<Props>): JSX.Element;
export default LiveProvider;
