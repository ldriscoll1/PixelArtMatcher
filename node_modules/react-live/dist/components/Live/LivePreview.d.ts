import React, { PropsWithChildren } from "react";
type Props = {
    Component?: React.ComponentType<PropsWithChildren<Record<string, unknown>>>;
};
declare function LivePreview({ Component, ...rest }: Props): JSX.Element;
export default LivePreview;
